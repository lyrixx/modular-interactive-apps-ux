{% cache "book-image-#{image}"  %}
    <img
        src="{{ data_uri_thumbnail(image, width, height) }}"
        class="img-fluid"
            {{ stimulus_controller('symfony/ux-lazy-image/lazy-image', {
                src: asset(image)
        }) }}
    />
{% endcache %}
